import React from "react";

import styles from "./styles.module.scss";

type Props = {};

const Footer = (props: Props) => {
  return <nav className={styles.nav}>Footer</nav>;
};

export default Footer;
